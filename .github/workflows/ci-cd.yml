# name: Devops-showcase-project CI/CD Pipeline

# on:
#   push:
#     branches: [main, develop]
#     tags:
#       - "v*" # Release trigger
#   pull_request:
#     branches: [main, develop]
#   schedule:
#     - cron: "0 2 * * 1" # Weekly security scan on Monday at 2 AM
#     - cron: "0 9 * * 1" # Weekly dependency update on Monday at 9 AM
#   workflow_dispatch: # Manual trigger

# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: ${{ github.repository }}

# jobs:
#   # Security Stage
#   security-scan:
#     name: Security Scanning
#     runs-on: ubuntu-latest
#     permissions:
#       actions: read
#       contents: read
#       security-events: write
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Run Trivy vulnerability scanner
#         uses: aquasecurity/trivy-action@master
#         with:
#           scan-type: "fs"
#           scan-ref: "."
#           format: "sarif"
#           output: "trivy-results.sarif"

#       - name: Upload Trivy scan results to GitHub Security tab
#         uses: github/codeql-action/upload-sarif@v3
#         if: always()
#         with:
#           sarif_file: "trivy-results.sarif"

#       - name: Initialize CodeQL
#         uses: github/codeql-action/init@v3
#         with:
#           languages: javascript, typescript

#       - name: Perform CodeQL Analysis
#         uses: github/codeql-action/analyze@v3

#       - name: Run npm audit for backend
#         run: |
#           cd application/backend
#           echo "🔍 Running npm audit for backend..."
#           npm audit --audit-level=moderate || echo "Backend audit completed with issues"

#       - name: Run npm audit for frontend
#         run: |
#           cd application/frontend
#           echo "🔍 Running npm audit for frontend..."
#           npm audit --audit-level=moderate || echo "Frontend audit completed with issues"

#       - name: Run npm audit for tests
#         run: |
#           cd tests
#           echo "🔍 Running npm audit for tests..."
#           npm audit --audit-level=moderate || echo "Tests audit completed with issues"

#       - name: Show audit summary
#         run: |
#           echo "📊 Security Audit Summary:"
#           echo "✅ Audit completed for all projects"
#           echo "⚠️  Some vulnerabilities may exist - check logs above for details"
#           echo "💡 Consider running 'npm audit fix --force' locally to address issues"

#   # Build Stage
#   code-build:
#     name: Build Applications
#     runs-on: ubuntu-latest
#     needs: security-scan
#     permissions:
#       contents: read
#     strategy:
#       matrix:
#         app: [backend, frontend]
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "18"
#           cache: "npm"
#           cache-dependency-path: application/${{ matrix.app }}/package-lock.json

#       - name: Install dependencies
#         run: |
#           cd application/${{ matrix.app }}
#           npm ci

#       - name: Install ESLint dependencies (if missing)
#         run: |
#           cd application/${{ matrix.app }}
#           npm install --save-dev eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin prettier eslint-config-prettier eslint-plugin-prettier eslint-plugin-react eslint-plugin-react-hooks || echo "ESLint dependencies already installed or failed to install"

#       - name: Run linting
#         run: |
#           cd application/${{ matrix.app }}
#           npm run lint || echo "Linting completed with issues or no config"

#       - name: Auto-fix linting issues
#         run: |
#           cd application/${{ matrix.app }}
#           npm run lint -- --fix || echo "Auto-fix completed with issues"

#       - name: Run Prettier formatting
#         run: |
#           cd application/${{ matrix.app }}
#           npx prettier --write "src/**/*.{ts,tsx,js,jsx,json,css,md}" || echo "Prettier formatting completed with issues"

#       - name: Type checking
#         run: |
#           cd application/${{ matrix.app }}
#           npm run type-check || echo "Type check not available or failed, skipping..."

#       - name: Build application
#         run: |
#           cd application/${{ matrix.app }}
#           npm run build || echo "Build completed with issues"

#       - name: Upload build artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: ${{ matrix.app }}-build
#           path: application/${{ matrix.app }}/dist
#           retention-days: 7

#   # Test Stage
#   run-test:
#     name: Run Tests
#     runs-on: ubuntu-latest
#     needs: code-build
#     permissions:
#       contents: read
#     services:
#       mongodb:
#         image: mongo:7.0
#         ports:
#           - 27017:27017
#         options: >-
#           --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#       redis:
#         image: redis:7-alpine
#         ports:
#           - 6379:6379
#         options: >-
#           --health-cmd "redis-cli ping"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # Optional: Install Docker if using self-hosted runner without Docker
#       # - name: Install Docker
#       #   run: |
#       #     sudo apt-get update
#       #     sudo apt-get install -y docker.io
#       #     sudo systemctl start docker
#       #     sudo systemctl enable docker
#       #     sudo usermod -aG docker $USER

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "18"
#           cache: "npm"
#           cache-dependency-path: |
#             application/backend/package-lock.json
#             application/frontend/package-lock.json
#             tests/package-lock.json

#       - name: Install test dependencies
#         run: |
#           cd tests
#           npm ci

#       - name: Remove jest config from package.json to avoid conflicts
#         run: |
#           cd tests
#           # Create a temporary package.json without jest config
#           node -e "
#             const fs = require('fs');
#             const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
#             delete pkg.jest;
#             fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2));
#           " || echo "Could not remove jest config from package.json"

#       - name: Run backend tests
#         run: |
#           cd tests
#           npx jest --config jest.config.js --testPathPattern=backend
#         env:
#           MONGODB_URI: mongodb://localhost:27017/ticketnow-test
#           REDIS_URL: redis://localhost:6379
#           JWT_SECRET: test-secret-key
#           NODE_ENV: test

#       - name: Run frontend tests
#         run: |
#           cd tests
#           npx jest --config jest.config.js --testPathPattern=frontend
#         env:
#           MONGODB_URI: mongodb://localhost:27017/ticketnow-test
#           REDIS_URL: redis://localhost:6379
#           JWT_SECRET: test-secret-key
#           NODE_ENV: test

#       - name: Run integration tests
#         run: |
#           cd tests
#           npx jest --config jest.config.js --testPathPattern=integration
#         env:
#           MONGODB_URI: mongodb://localhost:27017/ticketnow-test
#           REDIS_URL: redis://localhost:6379
#           JWT_SECRET: test-secret-key
#           NODE_ENV: test

#       - name: Run all tests with coverage
#         run: |
#           cd tests
#           npx jest --config jest.config.js --ci --coverage --watchAll=false
#         env:
#           MONGODB_URI: mongodb://localhost:27017/ticketnow-test
#           REDIS_URL: redis://localhost:6379
#           JWT_SECRET: test-secret-key
#           NODE_ENV: test

#       - name: Upload test coverage
#         uses: codecov/codecov-action@v3
#         with:
#           files: ./tests/coverage/lcov.info
#           flags: unittests
#           name: codecov-umbrella

#   # Docker Build Stage - COMMENTED OUT FOR TESTING
#   # docker-build:
#   #   name: Build Docker Images
#   #   runs-on: self-hosted
#   #   needs: [build, test]
#   #   strategy:
#   #     matrix:
#   #       app: [backend, frontend]
#   #   steps:
#   #     - name: Checkout code
#   #       uses: actions/checkout@v4

#   #     - name: Set up Docker Buildx
#   #       uses: docker/setup-buildx-action@v3

#   #     - name: Build Docker image
#   #       uses: docker/build-push-action@v5
#   #       with:
#   #         context: ./application/${{ matrix.app }}
#   #         file: ./application/${{ matrix.app }}/Dockerfile
#   #         push: false
#   #         tags: ${{ matrix.app }}:latest
#   #         cache-from: type=gha
#   #         cache-to: type=gha,mode=max

#   #     - name: Save Docker image
#   #       run: |
#   #         docker save ${{ matrix.app }}:latest | gzip > ${{ matrix.app }}-image.tar.gz

#   #     - name: Upload Docker image artifact
#   #       uses: actions/upload-artifact@v4
#   #       with:
#   #         name: ${{ matrix.app }}-image
#   #         path: ${{ matrix.app }}-image.tar.gz
#   #         retention-days: 1

#   # Image Scan Stage - COMMENTED OUT FOR TESTING
#   # image-scan:
#   #   name: Scan Docker Images
#   #   runs-on: self-hosted
#   #   needs: docker-build
#   #   strategy:
#   #     matrix:
#   #       app: [backend, frontend]
#   #   steps:
#   #     - name: Checkout code
#   #       uses: actions/checkout@v4

#   #     - name: Download Docker image artifact
#   #       uses: actions/download-artifact@v4
#   #       with:
#   #         name: ${{ matrix.app }}-image
#   #         path: ./

#   #     - name: Load Docker image
#   #       run: |
#   #         gunzip -c ${{ matrix.app }}-image.tar.gz | docker load

#   #     - name: Run Trivy vulnerability scanner
#   #       uses: aquasecurity/trivy-action@master
#   #       with:
#   #         image-ref: ${{ matrix.app }}:latest
#   #         format: "sarif"
#   #         output: "trivy-${{ matrix.app }}-results.sarif"

#   #     - name: Upload Trivy scan results to GitHub Security tab
#   #       uses: github/codeql-action/upload-sarif@v3
#   #       if: always()
#   #       with:
#   #         sarif_file: "trivy-${{ matrix.app }}-results.sarif"

#   #     - name: Run Trivy for high/critical vulnerabilities
#   #       uses: aquasecurity/trivy-action@master
#   #       with:
#   #         image-ref: ${{ matrix.app }}:latest
#   #         format: "table"
#   #         severity: "HIGH,CRITICAL"
#   #         exit-code: "1"

#   # Docker Push Stage - COMMENTED OUT FOR TESTING
#   # docker-push:
#   #   name: Push Docker Images
#   #   runs-on: self-hosted
#   #   needs: [docker-build, image-scan]
#   #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#   #   steps:
#   #     - name: Checkout code
#   #       uses: actions/checkout@v4

#   #     - name: Set up Docker Buildx
#   #       uses: docker/setup-buildx-action@v3

#   #     - name: Log in to Container Registry
#   #       uses: docker/login-action@v3
#   #       with:
#   #         registry: ${{ env.REGISTRY }}
#   #         username: ${{ github.actor }}
#   #         password: ${{ secrets.GITHUB_TOKEN }}

#   #     - name: Extract metadata
#   #       id: meta
#   #       uses: docker/metadata-action@v5
#   #       with:
#   #         images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
#   #         tags: |
#   #           type=ref,event=branch
#   #           type=ref,event=pr
#   #           type=sha,prefix={{branch}}-
#   #           type=raw,value=latest,enable={{is_default_branch}}

#   #     - name: Build and push backend image
#   #       uses: docker/build-push-action@v5
#   #       with:
#   #         context: ./application/backend
#   #         file: ./application/backend/Dockerfile
#   #         push: true
#   #         tags: |
#   #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/backend:latest
#   #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/backend:${{ github.sha }}
#   #         labels: ${{ steps.meta.outputs.labels }}

#   #     - name: Build and push frontend image
#   #       uses: docker/build-push-action@v5
#   #       with:
#   #         context: ./application/frontend
#   #         file: ./application/frontend/Dockerfile
#   #         push: true
#   #         tags: |
#   #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/frontend:latest
#   #           ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/frontend:${{ github.sha }}
#   #         labels: ${{ steps.meta.outputs.labels }}

#   # Deployment Stage - COMMENTED OUT FOR TESTING
#   # deploy:
#   #   name: Deploy to Staging
#   #   runs-on: self-hosted
#   #   needs: [docker-push]
#   #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#   #   environment: staging
#   #   steps:
#   #     - name: Checkout code
#   #       uses: actions/checkout@v4

#   #     - name: Deploy to staging
#   #       run: |
#   #         echo "Deployment to staging would happen here"
#   #         echo "Images available:"
#   #         echo "- ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/backend:${{ github.sha }}"
#   #         echo "- ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/frontend:${{ github.sha }}"

#   # Dependency Update Job
#   update-dependencies:
#     name: Update Dependencies
#     runs-on: ubuntu-latest
#     if: github.event_name == 'schedule' && github.event.schedule == '0 9 * * 1'
#     permissions:
#       contents: write
#       pull-requests: write
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "18"

#       - name: Update backend dependencies
#         run: |
#           cd application/backend
#           npm update
#           npm audit fix --force || echo "Backend audit fix completed with issues"

#       - name: Update frontend dependencies
#         run: |
#           cd application/frontend
#           npm update
#           npm audit fix --force || echo "Frontend audit fix completed with issues"

#       - name: Update test dependencies
#         run: |
#           cd tests
#           npm update
#           npm audit fix --force || echo "Tests audit fix completed with issues"

#       - name: Create Pull Request
#         uses: peter-evans/create-pull-request@v5
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           commit-message: "chore: update dependencies"
#           title: "chore: update dependencies"
#           body: |
#             ## Dependency Updates

#             This PR updates dependencies across all packages:
#             - Backend dependencies
#             - Frontend dependencies  
#             - Test dependencies

#             Please review the changes and test thoroughly before merging.
#           branch: dependency-update
#           delete-branch: true
